import numpy as np
import torch
from torch import nn
from torch.nn import functional as F

class SimpleListEncoder(object):
	"""docstring for SimpleListEncoder"""
	def __init__(self, emb_dim):
		super(SimpleListEncoder, self).__init__()
		
		self.
